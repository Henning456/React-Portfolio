import { Link } from "react-router-dom";
import { projects } from "../data/projects";
import { useParams } from "react-router-dom";

export default function DetailesProjects() {
  const { projectId } = useParams();
}
